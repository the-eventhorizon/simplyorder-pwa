import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'SimplyOrder Inventarisierung',
  webDir: 'dist',
};

export default config;
