import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.simplyorder.app',
  appName: 'SimplyOrder Inventarisierung',
  webDir: 'dist',
};

export default config;
